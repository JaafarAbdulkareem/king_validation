name: king_validation
description: A lightweight Flutter and Dart validation package with standalone validators, Form Validators, custom validation messages, and RegExp-based validation.
version: 1.2.0

homepage: https://github.com/JaafarAbdulkareem/king_validation
repository: https://github.com/JaafarAbdulkareem/king_validation
issue_tracker: https://github.com/JaafarAbdulkareem/king_validation/issues

topics:
  - validation
  - validator
  - forms
  - flutter
  - dart

environment:
  sdk: ">=3.8.1 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0