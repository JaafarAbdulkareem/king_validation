enum ValidationError {
  empty,

  invalidEmail,
  invalidPassword,
  invalidPhone,
  invalidUrl,
  invalidUsername,
  invalidName,
  invalidNumber,
  invalidOtp,
  invalidCreditCard,

  customValidationFailed,
}
